#: import XCamera kivy.garden.xcamera.XCamera

FloatLayout:
    orientation: "vertical"
    XCamera:
        id: xcamera

    Button:
        text: "change camera"
        size_hint: .2, .1
        pos_hint: {"center_x": .5}
